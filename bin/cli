#!/bin/sh

echo "Running via /bin/sh"

getpath() {
    [[ $1 = /* ]] && echo "$1" || echo "$PWD/${1#./}"
}

pathToDependency=$(getpath "$0")

if [[ -L $pathToDependency ]]; then
  echo "It's a symlink."
  pathToDir=$(readlink -f "$pathToDependency")
  pathToCli="$pathToDir/bin/cli-core.js"
else
  pathToCli="$pathToDependency-core.js"
fi



echo "Path to CLI: $pathToCli"

node $pathToCli "$@"
