#!/bin/sh

echo "Running via /bin/sh"

getpath() {
    [[ $1 = /* ]] && echo "$1" || echo "$PWD/${1#./}"
}

pathToCli="$( dirname $(getpath "$0") )/cli-core.js"

echo "Path to CLI: $pathToCli"

node $pathToCli "$@"
